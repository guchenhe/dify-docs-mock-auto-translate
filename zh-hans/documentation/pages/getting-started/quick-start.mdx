> ⚠️ 本文档由 AI 自动翻译。如有任何不准确之处，请参考[英文原版](../../../../en/documentation/pages/getting-started/quick-start.mdx)。

---
title: "10分钟快速开始"
description: "Dive into Dify through a quick example app"
icon: "forward"
---

Dify 的真正价值在于，无论多么复杂的 AI 想法，您都可以轻松构建、部署和扩展。它专为快速原型开发、流畅迭代以及任何级别的可靠生产部署而构建。

让我们首先学习如何将可靠的大型语言模型集成到您的应用程序中。在本指南中，您将构建一个简单的聊天机器人，它能对用户的问题进行分类，使用大型语言模型直接响应，并通过特定国家的趣味事实来增强响应。

<iframe 
  className="w-full aspect-video rounded-xl" 
  src="https://www.youtube.com/embed/opKZRpfd80k?si=HEkyjRpiYheMyrZ0" 
  title="Dify Quick Start Video" 
  frameBorder="0" 
  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
  allowFullScreen 
/>

## 步骤 1：创建新工作流（2分钟）

1. 前往 **Studio** > **工作流** > **从空白创建** > **编排** > **新对话流** > **创建**

## 步骤 2：添加工作流节点（6分钟）

<Tip>
  当您想要引用任何变量时，先输入 `{` 或 `/`，您就可以看到工作流中可用的不同变量。
</Tip>

### 1. 大型语言模型节点和输出：理解并回答问题

<Info>
  `LLM` 节点向语言模型发送提示词以基于用户输入生成响应。它抽象了 API 调用、速率限制和基础设施的复杂性，因此您可以专注于设计逻辑。
</Info>

<Steps>
  <Step title="创建大型语言模型节点">
    使用 `添加节点` 按钮创建一个大型语言模型节点并将其连接到您的开始节点
  </Step>
  
  <Step title="配置模型">
    选择默认模型
  </Step>
  
  <Step title="设置系统提示词">
    将以下内容粘贴到系统提示词字段中：

    ```text
    The user will ask a question about a country. The question is {{sys.query}}  
    Tasks: 
    1. Identify the country mentioned. 
    2. Rephrase the question clearly. 
    3. Answer the question using general knowledge. 
    
    Respond in the following JSON format: 
    {   
      "country": "<country name>",
      "question": "<rephrased question>",
      "answer": "<direct answer to the question>" 
    }
    ```
  </Step>
  
  <Step title="启用结构化输出">
    **启用结构化输出** 允许您轻松控制大型语言模型将返回的内容，并确保输出一致、机器可读，以便在下游用于精确的数据提取或条件逻辑。
    
    - 打开输出变量结构化开关 > `配置` 并点击 `从 JSON 导入`
    - 粘贴：
    
    ```json
    {   
      "country": "string",   
      "question": "string",   
      "answer": "string" 
    }
    ```
  </Step>
</Steps>

### 2. 代码块：获取使用代码执行自定义逻辑。它让您能够在需要的地方准确注入代码——在可视化工作流中——为您节省了构建整个后端的工作。
</Info>

<Steps>
  <Step title="创建代码节点">
    使用 `添加节点` 按钮创建一个 `Code` 节点并连接到大型语言模型块
  </Step>
  
  <Step title="配置输入变量">
    将一个 `输入变量` 名称更改为 "country" 并将变量设置为 `structured_output` > `country`
  </Step>
  
  <Step title="添加 Python 代码">
    将此代码粘贴到 `PYTHON3` 中：

    ```python
    def main(country: str) -> dict:     
      country_name = country.lower()     
      fun_facts = {
        "japan": "Japan has more than 5 million vending machines.",
        "france": "France is the most visited country in the world.",
        "italy": "Italy has more UNESCO World Heritage sites than any other country."     
      }     
      fun_fact = fun_facts.get(country_name, f"No fun fact available for {country.title()}.")
      return {"fun_fact": fun_fact}
    ```
  </Step>
  
  <Step title="重命名输出变量">
    将输出变量 `result` 更改为 `fun_fact` 以获得更好的变量标签
  </Step>
</Steps>

### 3. 回答节点：给用户的最终答案

<Info>
  `Answer` 节点创建一个干净的最终输出以返回。
</Info>

<Steps>
  <Step title="创建回答节点">
    使用 `添加节点` 按钮创建一个 `Answer` 节点
  </Step>
  
  <Step title="配置回答字段">
    粘贴到回答字段中：

    ```text
    Q: {{ structured_output.question }} 

    A: {{ structured_output.answer }} 

    Fun Fact: {{ fun_fact }}
    ```
  </Step>
</Steps>

结束工作流：

<img
  src="/images/quick-start-workflow-overview.png"
  alt="Complete workflow diagram showing LLM, Code, and Answer nodes connected"
  style={{ width: "100%" }}
/>

---

## 步骤 3：测试机器人（3分钟）

点击 `预览`，然后询问：

- "法国的首都是什么？"
- "告诉我关于日本料理的事情"
- "描述意大利的文化"
- 任何其他问题

确保您的机器人按预期工作！

## 您已完成机器人构建！

本指南展示了如何可靠且可扩展地集成语言模型，而无需重新发明基础设施。通过 Dify 的可视化工作流和模块化节点，您不仅构建得更快，还采用了用于大型语言模型驱动应用程序的干净、生产就绪的架构。

{/*
Contributing Section
DO NOT edit this section!
It will be automatically generated by the script.
*/}

---

[编辑此页面](https://github.com/langgenius/dify-docs/edit/main/en/documentation/pages/getting-started/quick-start.mdx) | [报告问题](https://github.com/langgenius/dify-docs/issues/new?template=docs.yml)